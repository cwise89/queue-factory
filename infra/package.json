{
  "name": "bria-sms-cdk",
  "version": "1.0.0",
  "bin": {
    "app": "bin/app.js"
  },
  "scripts": {
    "cdk": "npx cdk",
    "synth": "npx cdk synth",
    "deploy:dev": "npx cdk deploy --tags component=bria --tags project=bria-sms  --progress events --no-rollback",
    "deploy:prod": "npx cdk deploy --tags component=bria --tags project=bria-sms --progress events"
  },
  "devDependencies": {
    "@types/node": "^10.17.27",
    "aws-cdk": "^2.51.1",
    "aws-cdk-lib": "^2.51.1",
    "constructs": "^10.0.0",
    "ts-node": "^10.9.1",
    "typescript": "~3.9.7"
  }
}
